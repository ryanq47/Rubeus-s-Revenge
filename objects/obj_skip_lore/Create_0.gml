// Set button dimensions (assuming sprite is already assigned)
button_width = sprite_width;
button_height = sprite_height;

// Define a state for the button
button_state = "idle";  // You can have states like "idle", "hover", "pressed"
